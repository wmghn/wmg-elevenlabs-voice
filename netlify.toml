[build]
  publish = "public"

# Trình duyệt gọi thẳng api.elevenlabs.io (API hỗ trợ CORS) — không dùng Netlify
# Function làm proxy: function bị giới hạn 10 giây, audio dài sẽ dính 504.

# SPA fallback
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
